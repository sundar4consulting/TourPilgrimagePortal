{
  "name": "pilgrimage-portal",
  "version": "1.0.0",
  "description": "A comprehensive web portal for Pilgrimage Tour Operator with member and admin portals",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["pilgrimage", "tour", "travel", "expense", "portal"],
  "author": "Vishnu Chitr",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}